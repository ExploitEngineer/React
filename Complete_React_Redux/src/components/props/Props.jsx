// Definition: Props are read-only data passed from parent to child components, enabling one-way data flow.

import Parent from './Parent';

export default function Props() {
    return <Parent />
};
