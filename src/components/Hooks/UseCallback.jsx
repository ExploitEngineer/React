const UseCallback = ({ Learning }) => {

    // What is useCallback and why we use it ?
    // How to use useCallback with example ?

    function useCallbkFun() {
        console.log("useCallback Component");
    }
    useCallbkFun();

    return (
        <>
        </>
    )
}

export default UseCallback;
